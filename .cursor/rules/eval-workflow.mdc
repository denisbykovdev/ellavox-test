---
description: Eval workflow — one story at a time, STORIES.md is source of truth
alwaysApply: true
---

# Eval workflow

Source of truth is **acceptance criteria in `STORIES.md`**, not `Intentional...` comments, easter-egg notes, or existing tests. Comments can be wrong; AC is not.

## Process

- Work **one story at a time**. Do not mix unrelated fixes from other stories.
- Read the story AC first, then write/adjust tests for those criteria, then implement.
- After each story: run `npm test`, `npm run lint`, and `npm run typecheck`. All three must pass.
- Update `PROGRESS.md` with that story’s status, what changed, and remaining gaps.

## Shared logic

- Phone, time/TTL, sanitization, and other cross-skill helpers live in `packages/shared`.
- Skills and the gateway consume shared helpers; do not keep a second competing implementation.
- US phones: only `normalizePhone` in `packages/shared`. Strip non-digits, then 10 digits → `+1XXXXXXXXXX`, 11 digits starting with `1` → `+1XXXXXXXXXX` (never `+1` + 11 digits).
- Inbound text: only `sanitizeInboundText` in `packages/shared`. CRLF/`\r` → LF; `\u2028`/`\u2029` → LF; `trimEnd` per line; never collapse newlines to spaces. Gateway runs it on `/message` before skills.

## Public contracts

- Do not break exported APIs (`Skill`, `MessageContext`, `SkillResult`, `isPairingCodeValid`, HTTP success payload fields).
- Additive changes only (new optional fields, new routes, new skills). Existing skills must keep working unmodified.
- Unknown-skill errors: keep a backward-compatible response shape while meeting Story 6 AC.

## Secrets and time

- Never put `process.env`, tokens, keys, or other secrets into `/healthz`, logs, or error bodies.
- `/healthz` returns only `{ status, uptimeSeconds, skillsLoaded, version }`. Read `version` from `apps/gateway/package.json` (not a hardcoded string). Keep `GET /health` as `{ ok, sessions }`.
- Tests must be time-deterministic: inject `nowMs` / a fake clock. No real `sleep` / `setTimeout` waits for TTL or expiry.
- Compare durations in **milliseconds only** (never mix with seconds). Pairing: 10 min TTL, ±15s skew, **inclusive** on both ends (`ageMs >= -15s && ageMs <= 10min + 15s`).
- Session TTL: `SESSION_TTL_SECONDS` default 3600. Expired when `nowMs - lastSeenAt >= ttlSeconds * 1000` → HTTP 401. Inject `now` into `createGateway`; do not import `index.ts` in tests (it listens).
- If an existing test contradicts `STORIES.md`, rewrite the test to the AC. Do not implement the old expectation.

## Commit messages

After a story, print a **copy-pasteable** message: type line, then a bullet for **every completed step** (not a one-line summary). The user pastes it as-is; do not ask them to send it back.

```
bug-fix: <short headline>;

- step one;
- step two;
- step three;
```

Example (Story 2):

```
bug-fix: normalize US phones in shared and drop the report duplicate;

- move US normalization to packages/shared normalizePhone (+1XXXXXXXXXX);
- fix double +1 on 11-digit numbers;
- accept multiple input formats (10 digits, +E.164, punctuation);
- remove duplicate normalizePhoneNumber from report and import shared;
- add edge-case tests (10 digits, 11 with leading 1, already +, junk chars);
```
